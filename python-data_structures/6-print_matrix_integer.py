#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for i in matrix:
        for num in i:
            if num != i[-1]
                print("{:d}".format(num), end=" ")
            else:
                print("")
        print()
