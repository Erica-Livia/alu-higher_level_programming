#!/usr/bin/python3
import __format__
def islower(c):
    for i in range(ord('a'), ord('z') + 1):
        "{:c}".format(islower)
        if "{:c}".format(islower) == True:
            return True
        else:
            return False
